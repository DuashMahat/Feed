 platform :ios, '9.0'

target 'Feed' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Feed
  pod 'SDWebImage', '~> 5.0'
  target 'FeedTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
